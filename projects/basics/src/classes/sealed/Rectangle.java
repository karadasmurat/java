package classes.sealed;

public non-sealed class Rectangle extends Shape { // The class with a sealed direct supertype should be declared either
                                                  // final, sealed or non-sealed

}
